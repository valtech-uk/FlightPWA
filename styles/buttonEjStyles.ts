import { CSSProperties } from "@mui/material/styles/createMixins";

const buttonEjStyles: CSSProperties = {
  borderRadius: "100px",
  margin: "2rem auto",
  height: "2.5rem",
  boxShadow: "none",
};

export default buttonEjStyles;
